{
  "name": "percolator-sov",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @percolator/core test",
    "dev:cli": "pnpm --filter @percolator/cli dev",
    "dev:app": "pnpm --filter @percolator/app dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.4"
  }
}
