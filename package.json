{
  "name": "viper",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @viper/core test",
    "dev:cli": "pnpm --filter @viper/cli dev",
    "dev:app": "pnpm --filter @viper/app dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.4"
  }
}
